document.getElementById('codeInputBlock').addEventListener('load', removeWhiteSpaceFunction);

const codeBlock
function removeWhiteSpaceFunction() {
    console.log('hello from remove white space function');
    var codeInputBloc = document.getElementById('codeInputBlock');

    // codeInputBloc.nextSibling.parentNode.removeChild(element.nextSibling);
}